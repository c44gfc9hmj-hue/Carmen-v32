Carmen v49.9 (49.9-identity-queue-visual) — identity + queue + visual gate
==========================================================================
Parent: 9cb6289 (v49.8 adaptive-investigation)

Incremental production fix. Extends the v49.8 planner + worker engine.
Does not rebuild Carmen or replace retrieval.

- PERSON identity-first verification pack (small candidate set, multiple Yes,
  “Not this person” is negative evidence).
- Persistent investigation queue survives Worker resource/time rails and is
  resumable. StopKind E never says “Research complete.”
- Visual evidence gate: entity + topic + visual relevance. UNVERIFIED VISUAL
  when Carmen cannot verify. Find More returns unique relevant visuals or none.
- Research Focus PERSON/VISUALS/TUTORIAL/CLOTHING/POSITION/URL/TOPIC are real
  planner branches. Adaptive Lens follows focus. Topic follow-up is entity×topic.
- Analyze investigates public account metadata only. Never bypasses auth.
- Deep Dive snaps/transitions into the investigation view.

Build identifier: **49.9-identity-queue-visual**.
