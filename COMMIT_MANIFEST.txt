Carmen v50 (50-agent-testable-progressive) — agent API, thumbnails, bondage diagnostics, progressive dive
================================================================================
Parent: d86e3ea (v49.14 person-selection + image-result production fix)

Incremental production pass. Extends the v49.14 planner + worker engine.
Does not rebuild Carmen. Does not redesign Deep Dive. Does not mock retrieval.

- Authorized AI assistants drive the SAME runDiscovery pipeline via
  /api/v1/machine/search, /candidates, /confirm, /dive, /diagnostics.
- personCandidates expose id, displayName, sourceUrl, sourceTitle,
  thumbnailUrl (from THAT candidate's source only), confidence, whySelected.
- Bondage zero-visuals are diagnosed (providers_returned_zero /
  retrieval_failed / filtered_out / incorrectly_classified_or_rejected /
  ui_or_serialization_dropped / all_rejected_by_visual_gate).
- Person×topic image-index hits whose title omitted the name stay UNVERIFIED
  (gate query-associated), never VERIFIED, never a stolen first image.
- Deep Dive parallelizes web variants in batches of 3; stage=initial returns
  first useful results so the UI is not blank while slower sources finish.
- Request IDs, per-stage timings, provider/filter/verify/dedupe counts are on
  every discovery envelope (PWA GET /search and machine API).
- v49.14 identity surfaces, sourceUrl vs imageUrl, and unique same-page
  images are preserved.

Build identifier: **50-agent-testable-progressive**.

v50 auth + ChatGPT MCP (same VERSION 50 / 50-agent-testable-progressive)
-----------------------------------------------------------------------
Does not redesign search, Deep Dive, thumbnails, Bondage, Visuals, Find More, or Ask.

- Machine routes require dedicated Worker secret CARMEN_API_KEY (never API_KEY).
- Unauthorized POST /api/v1/machine/* → 401 WWW-Authenticate Bearer. PWA GET /search, POST /dive, POST /analyze stay public.
- Deploy generate-if-missing: if GitHub secret CARMEN_API_KEY is empty and the Worker has none, wrangler secret put a generated key so production is not left open. User-held GitHub secret is required for ChatGPT.
- ChatGPT integration layer: Streamable HTTP MCP at /mcp wrapping the same handleCarmenApi/runDiscovery tools; OAuth 2.1 (PKCE + DCR) at /oauth/* plus /.well-known/oauth-protected-resource so Developer Mode connectors can connect. Custom GPT Actions remain documented until GPTs retire.
- Public GET /api/v1/chatgpt-setup and /privacy. This Grok session cannot log into ChatGPT.
