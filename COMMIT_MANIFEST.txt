Carmen v49.14 (49.14-person-image-results) — person selection + image results
================================================================================
Parent: 4f33eae (revert VERSION bump until v49.14 sources land)

Incremental production fix. Extends the v49.13 planner + worker engine.
Does not rebuild Carmen. Does not redesign Deep Dive.

- Distinct identity surfaces stay distinct unless they corroborate.
  Sharing an unspecified role is not a merge key.
- Each presented person candidate carries sourceUrl / sampleUrl.
- Open/View Source uses the stored source URL, not the image URL.
- Thumbnails come only from that candidate's own images.
- Image-index hits keep imageUrl and sourceUrl separate.
- Multiple unique images survive; duplicates collapse by image identity.
- Same-host / same-gallery-page images are not collapsed into one result.
- Provider/retrieval metadata survives on visual hits.
- Unverified images stay unlabeled as verified.
- Incomplete apply-v49.14.sh hook removed; sources land in the tree.

Build identifier: **49.14-person-image-results**.
