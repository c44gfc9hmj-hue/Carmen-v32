Carmen v49.4 (49.4-deep-dive-lenses) — ChatGPT Actions contract
==============================================================
Parent: d2efd7bb962e9e0a7067576f86317cead15b6f7e (v49.4 machine API)

Make the live Machine API actually importable by a ChatGPT Custom GPT Action.
Fold the CI connector-pass into worker.js. Expand OpenAPI 3.0.3 (Bearer first,
full request/response schemas, x-openai-isConsequential: false, confirm-identity).
Accept investigationState as object or JSON string. Echo investigationStateJson.
Canonical agent sequence in API.md / CHATGPT.md. Production smoke script.

No secrets. No PWA redesign. No provider API_KEY reuse. External actions stay 403.
Worker memory is still not durable — agents must echo investigationState.

Build identifier remains **49.4-deep-dive-lenses**.
